<%= render 'principal/header' %>
<% @title = t('.inicio') %>

  <section class="home" id="inicio" data-stellar-background-ratio="0.4">
    <div class="container">
      <div class="row">
        <div class="col-md-12">
          <div class="st-home-unit">
            <div class="hero-txt">
              <p class="hero-work">El Colegio de México</p>
              <img src="images/logo-gde-<%= I18n.locale == 'es' ? 'esp' : 'eng' %>-bc.png" alt="Programa Internacional de Verano" class="img-responsive"><br>
              <br>
              <a class="btn btn-main btn-lg" href="https://www.youtube.com/embed/44lAU_83D8E?autoplay=1" target="_blank"><%= t('.conoce') %></a> </div>
          </div>
        </div>
      </div>
    </div>
    <div class="mouse-icon">
      <div class="wheel"></div>
    </div>
  </section>
  <section class="call-us">
    <div class="container">
      <div class="row">
        <div class="col-md-12">
          <h3><%= t('.folleto') %></h3>
          <a href="<%= @folleto %>" class="btn btn-default-o btn-lg" target="_blank"><%= t('.aqui').upcase %></a> </div>
      </div>
    </div>
  </section>
  <section class="about" id="programa">
    <div class="container">
      <div class="row">
        <div class="col-md-12">
          <div class="section-title st-center">
            <h3><%= t('.sobre') %></h3>
            <p><%= t('.curso') %></p>
          </div>
          <div class="row mb90">
            <div class="col-md-6">
              <%= t('.desc_curso_html') %>
              <a href="<%= (I18n.locale == :es ? programa_path : programa_en_path) %>" class="btn btn-main btn-lg"><%= t('.mas_info') %></a> </div>
            <div class="col-md-6">
              <h3 class="text-center"><img src="images/alumnos.jpg" alt="" class="img-responsive"></h3>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
  <section class="call-2-acction" data-stellar-background-ratio="0.4" id="colmex">
    <div class="container">
      <div class="row">
        <div class="col-md-12">
          <div class="c2a">
            <h2><%= t('.colegio') %></h2>
            <p><%= t('.desc_colmex') %></p>
            <a href="<%= (I18n.locale == :es ? sobre_path : sobre_en_path) %>" class="btn btn-main btn-lg"><%= t('.info') %></a> </div>
        </div>
      </div>
    </div>
  </section>
  <section class="pricing" id="calendario">
    <div class="container">
      <div class="row">
        <div class="col-md-12">
          <div class="section-title st-center">
            <h3><%= t('.fecha') %></h3>
            <p><%= t('.calendario') %></p>
          </div>
          <table class="table table-striped table-bordered resaltado">
            <tbody>
<% @fechas.each do |f| %>
              <tr style="<%= f.fecha_i <= Date.current && f.fecha_f >= Date.current ? 'background-color: #7c334e; color: #fff;' : '' %>">
                <td style="text-align: center" class="verde">
								<% if f.fecha_f < Date.current %>
                  <i class="fa fa-check"></i>
								<% end %>
                </td>
                <td><%= I18n.locale == :es ? f.desc_es : f.desc_en %></td>
                <td class="<%= f.fecha_i <= Date.current && f.fecha_f >= Date.current ? '' : 'verde' %>"><%= "#{f.fecha_i != f.fecha_f && f.fecha_i <= Date.current && f.fecha_f >= Date.current ? '<strong>' : ''}#{f.fecha_i != f.fecha_f ? t('.inicial') : ''}#{l f.fecha_i, format: (f.fecha_i.year == Date.current.year ? :no_year : :with_year)}#{f.fecha_i != f.fecha_f ? t('.rango') + l(f.fecha_f, format: (f.fecha_i.year == Date.current.year ? :no_year : :with_year)) : ''}#{f.fecha_i != f.fecha_f && f.fecha_i <= Date.current && f.fecha_f >= Date.current ? '</strong>' : ''}".html_safe %></td>
              </tr>
<% end %>

            </tbody>
          </table>
        </div>
      </div>
    </div>
  </section>

<%= render 'principal/footer' %>
